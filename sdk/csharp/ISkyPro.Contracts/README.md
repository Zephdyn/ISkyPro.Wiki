# ISkyPro.Contracts

Shared public contracts for ISkyPro Plugin SDK v2, including plugin manifests,
initialize negotiation, event envelopes, acknowledgements, and Bot context
models. Most plugin authors should reference `ISkyPro.PluginSdk`, which brings
this package in as a dependency.
